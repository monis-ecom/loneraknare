{
  "name": "nv-design-kit",
  "version": "0.1.0",
  "private": true,
  "description": "React mirror of the AS Product Widgets (Nordiska Varuhuset) section library. Each component emits the exact same BEM markup as its Elementor/PHP widget and is styled by the real nv-product-widgets.css, so Claude Design can mock up on-brand landing pages that map 1:1 onto the shipping WordPress plugin.",
  "type": "module",
  "module": "dist/index.es.js",
  "main": "dist/index.es.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.es.js"
    },
    "./styles.css": "./src/styles/nv-kit.css"
  },
  "sideEffects": [
    "*.css"
  ],
  "scripts": {
    "build": "node build.mjs && tsc --emitDeclarationOnly"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.23.1",
    "playwright": "^1.47.0",
    "playwright-core": "^1.47.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.6.3"
  }
}
